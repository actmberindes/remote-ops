import React from 'react';

// Live View timelapse generation has been retired.
// Kept as a no-op compatibility component so existing App.jsx imports do not
// break while the UI feature is removed from active use.
export default function LiveViewVideoPlayer() {
  return null;
}
